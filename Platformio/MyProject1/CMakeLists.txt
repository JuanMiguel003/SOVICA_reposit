cmake_minimum_required(VERSION 3.16.0)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

#incluye blibliotecas que estan en la carpeta components
#list(APPEND EXTRA_COMPONENT_DIRS include)

project(MyProject1)
